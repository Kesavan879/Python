class Solution(object):
    def hasCycle(self, head):
        while head:
            if head.val==float('inf'):
                return True
            head.val = float('inf')
            head=head.next
        return False
